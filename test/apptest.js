const assert=require('chai').assert;
const app=require('../app');
describe('Check UserName from app', () => {
    it('should be equal to or greater than 3 char', () => {
        
     assert.equal(app.'hello');
    })
   })